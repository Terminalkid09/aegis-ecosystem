"""Parser di sorgente, uno per famiglia di log.

Ogni modulo implementa `Parser` (vedi `app.ingest.base`). Aggiungere una
sorgente = aggiungere un file qui e registrarlo in `app.ingest.registry`.
"""
