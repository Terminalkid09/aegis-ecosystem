[pytest]
testpaths = tests
asyncio_mode = auto
# pytest-asyncio 1.x: un solo loop per sessione (come il vecchio fixture
# event_loop custom, rimosso perché non più supportato).
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
